[pytest]
pythonpath = .
testpaths = selenium_tests playwright_tests
addopts = --durations=0 --durations-min=0.1 --alluredir=allure-results
markers =
    flaky: optional reruns for known-flaky tests (pytest-rerunfailures)
